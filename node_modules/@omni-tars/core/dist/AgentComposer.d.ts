import { AgentPlugin } from './AgentPlugin';
import Agent, { LLMRequestHookPayload, LLMResponseHookPayload } from '@tarko/agent';
/**
 * Composes multiple agent plugins into a unified system prompt and instruction set
 */
export declare class AgentComposer {
    private plugins;
    private logger;
    constructor(option: {
        plugins: AgentPlugin[];
    });
    /**
     *
     * @param agent Inject agent instance into all plugins that support it
     */
    setAgent(agent: Agent): void;
    /**
     * Initialize all agent plugins
     */
    initialize(): Promise<void>;
    /**
     * Generate the composed system prompt from all agent plugins
     */
    generateSystemPrompt(): string;
    /**
     * Get all tools from all agent plugins
     */
    getAllTools(): import("@tarko/agent").Tool<any, any>[];
    /**
     * Execute onLLMRequest hooks for all plugins
     */
    executeOnLLMRequest(id: string, payload: LLMRequestHookPayload): Promise<void>;
    /**
     * Execute onLLMResponse hooks for all plugins
     */
    executeOnLLMResponse(id: string, payload: LLMResponseHookPayload): Promise<void>;
    /**
     * Execute onEachAgentLoopStart hooks for all plugins
     */
    executeOnEachAgentLoopStart(): Promise<void>;
    /**
     * Execute onEachAgentLoopEnd hooks for all plugins
     */
    executeOnEachAgentLoopEnd(): Promise<void>;
    /**
     * Execute onAgentLoopEnd hooks for all plugins
     */
    executeOnAgentLoopEnd(): Promise<void>;
    /**
     * Execute onAfterToolCall hooks for all plugins
     */
    executeOnAfterToolCall(id: string, toolCall: {
        toolCallId: string;
        name: string;
    }, result: unknown): Promise<any>;
    /**
     * Get list of available environments from plugins
     */
    private getAvailableEnvironments;
    /**
     * Check if a specific plugin type is available
     */
    private hasPlugin;
    /**
     * Generate usage instructions based on available plugins
     */
    private generateUsageInstructions;
}
//# sourceMappingURL=AgentComposer.d.ts.map