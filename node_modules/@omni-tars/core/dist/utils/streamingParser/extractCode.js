/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    extractCodeEnv: ()=>extractCodeEnv
});
function extractCodeEnv(content, state) {
    if (!state.codeEnvBuffer) state.codeEnvBuffer = '';
    state.codeEnvBuffer += content;
    let hasToolCallUpdate = false;
    const streamingToolCallUpdates = [];
    while(state.codeEnvBuffer.length > 0 && !state.insideGUI && !state.insideMcp){
        if (state.codeEnvBuffer.indexOf('computer_env') > -1) {
            state.insideGUI = true;
            break;
        }
        if (state.codeEnvBuffer.indexOf('mcp_env') > -1) {
            state.insideMcp = true;
            break;
        }
        if (state.insideCodeEnv) {
            const closeMatch = state.codeEnvBuffer.match(/<\/code_env>/);
            if (state.insideFunction) {
                const functionCloseMatch = state.codeEnvBuffer.match(/<\/function>/);
                if (state.insideParameter) {
                    const parameterCloseMatch = state.codeEnvBuffer.match(/<\/parameter>/);
                    if (parameterCloseMatch) {
                        const paramValue = state.codeEnvBuffer.substring(0, parameterCloseMatch.index);
                        if (state.currentParameterName) state.currentParameters[state.currentParameterName] = (state.currentParameters[state.currentParameterName] || '') + paramValue;
                        state.codeEnvBuffer = state.codeEnvBuffer.substring(parameterCloseMatch.index + parameterCloseMatch[0].length);
                        state.insideParameter = false;
                        const toolCall = state.toolCalls.find((tc)=>tc.id === state.currentToolCallId);
                        if (toolCall) {
                            const escapedValue = paramValue.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t');
                            toolCall.function.arguments += escapedValue + '"';
                        }
                        hasToolCallUpdate = true;
                        streamingToolCallUpdates.push({
                            toolCallId: state.currentToolCallId || '',
                            toolName: state.currentToolName || '',
                            argumentsDelta: paramValue.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t') + '"',
                            isComplete: false
                        });
                        continue;
                    }
                    if (hasPartialTagAtEnd(state.codeEnvBuffer, [
                        '</parameter>'
                    ])) {
                        const lastBracketIndex = state.codeEnvBuffer.lastIndexOf('<');
                        const paramValue = state.codeEnvBuffer.substring(0, lastBracketIndex);
                        if (paramValue && state.currentParameterName) {
                            state.currentParameters[state.currentParameterName] = (state.currentParameters[state.currentParameterName] || '') + paramValue;
                            const toolCall = state.toolCalls.find((tc)=>tc.id === state.currentToolCallId);
                            if (toolCall) {
                                const escapedValue = paramValue.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t');
                                toolCall.function.arguments += escapedValue;
                            }
                            hasToolCallUpdate = true;
                            streamingToolCallUpdates.push({
                                toolCallId: state.currentToolCallId || '',
                                toolName: state.currentToolName || '',
                                argumentsDelta: paramValue.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t'),
                                isComplete: false
                            });
                        }
                        state.codeEnvBuffer = state.codeEnvBuffer.substring(lastBracketIndex);
                    } else {
                        if (state.currentParameterName && state.codeEnvBuffer) {
                            state.currentParameters[state.currentParameterName] = (state.currentParameters[state.currentParameterName] || '') + state.codeEnvBuffer;
                            const toolCall = state.toolCalls.find((tc)=>tc.id === state.currentToolCallId);
                            if (toolCall) {
                                const escapedValue = state.codeEnvBuffer.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t');
                                toolCall.function.arguments += escapedValue;
                            }
                            hasToolCallUpdate = true;
                            streamingToolCallUpdates.push({
                                toolCallId: state.currentToolCallId || '',
                                toolName: state.currentToolName || '',
                                argumentsDelta: state.codeEnvBuffer.replace(/\\/g, '\\\\').replace(/"/g, '\\"').replace(/\n/g, '\\n').replace(/\r/g, '\\r').replace(/\t/g, '\\t'),
                                isComplete: false
                            });
                        }
                        state.codeEnvBuffer = '';
                    }
                    break;
                }
                {
                    const parameterMatch = state.codeEnvBuffer.match(/<parameter=([^>]+)>/);
                    if (parameterMatch) {
                        const paramName = parameterMatch[1];
                        state.currentParameterName = paramName;
                        state.insideParameter = true;
                        state.parameterBuffer = '';
                        state.codeEnvBuffer = state.codeEnvBuffer.substring(parameterMatch.index + parameterMatch[0].length);
                        const isFirstParam = !state.currentParameters || 0 === Object.keys(state.currentParameters).length;
                        const argsDelta = isFirstParam ? `{"${paramName}": "` : `, "${paramName}": "`;
                        const toolCall = state.toolCalls.find((tc)=>tc.id === state.currentToolCallId);
                        if (toolCall) toolCall.function.arguments += argsDelta;
                        hasToolCallUpdate = true;
                        streamingToolCallUpdates.push({
                            toolCallId: state.currentToolCallId || '',
                            toolName: state.currentToolName || '',
                            argumentsDelta: argsDelta,
                            isComplete: false
                        });
                        continue;
                    }
                    if (functionCloseMatch) {
                        state.codeEnvBuffer = state.codeEnvBuffer.substring(functionCloseMatch.index + functionCloseMatch[0].length);
                        const toolCall = state.toolCalls.find((tc)=>tc.id === state.currentToolCallId);
                        if (toolCall && !toolCall.function.arguments.endsWith('}')) {
                            toolCall.function.arguments += ' }';
                            hasToolCallUpdate = true;
                            streamingToolCallUpdates.push({
                                toolCallId: state.currentToolCallId || '',
                                toolName: state.currentToolName || '',
                                argumentsDelta: ' }',
                                isComplete: true
                            });
                        }
                        state.insideFunction = false;
                        state.currentToolName = '';
                        continue;
                    }
                    if (closeMatch) {
                        state.codeEnvBuffer = state.codeEnvBuffer.substring(closeMatch.index + closeMatch[0].length);
                        state.insideCodeEnv = false;
                        state.insideFunction = false;
                        continue;
                    } else {
                        if (hasPartialTagAtEnd(state.codeEnvBuffer, [
                            '<parameter=',
                            '</function>',
                            '</code_env>'
                        ])) {
                            const lastBracketIndex = state.codeEnvBuffer.lastIndexOf('<');
                            state.codeEnvBuffer = state.codeEnvBuffer.substring(lastBracketIndex);
                        } else state.codeEnvBuffer = '';
                        break;
                    }
                }
            }
            {
                const functionMatch = state.codeEnvBuffer.match(/<function=([^>]*)>/);
                if (functionMatch && functionMatch[1]) {
                    const toolName = functionMatch[1];
                    state.currentToolName = toolName;
                    state.currentParameters = {};
                    state.insideFunction = true;
                    state.codeEnvBuffer = state.codeEnvBuffer.substring(functionMatch.index + functionMatch[0].length);
                    if (!state.currentToolCallId) state.currentToolCallId = generateToolCallId();
                    const existingToolCallIndex = state.toolCalls.findIndex((tc)=>tc.id === state.currentToolCallId);
                    if (-1 === existingToolCallIndex) state.toolCalls.push({
                        id: state.currentToolCallId,
                        type: 'function',
                        function: {
                            name: toolName,
                            arguments: ''
                        }
                    });
                    hasToolCallUpdate = true;
                    streamingToolCallUpdates.push({
                        toolCallId: state.currentToolCallId,
                        toolName: toolName,
                        argumentsDelta: '',
                        isComplete: false
                    });
                    continue;
                }
                if (closeMatch) {
                    state.codeEnvBuffer = state.codeEnvBuffer.substring(closeMatch.index + closeMatch[0].length);
                    state.insideCodeEnv = false;
                    continue;
                }
                if (hasPartialTagAtEnd(state.codeEnvBuffer, [
                    '<function=',
                    '</code_env>'
                ])) {
                    const lastBracketIndex = state.codeEnvBuffer.lastIndexOf('<');
                    state.codeEnvBuffer = state.codeEnvBuffer.substring(lastBracketIndex);
                } else state.codeEnvBuffer = '';
                break;
            }
        }
        {
            const openMatch = state.codeEnvBuffer.match(/<code_env>/);
            if (openMatch) {
                state.codeEnvBuffer = state.codeEnvBuffer.substring(openMatch.index + openMatch[0].length);
                state.insideCodeEnv = true;
                continue;
            }
            if (hasPartialTagAtEnd(state.codeEnvBuffer, [
                '<code_env>'
            ])) {
                const lastBracketIndex = state.codeEnvBuffer.lastIndexOf('<');
                state.codeEnvBuffer = state.codeEnvBuffer.substring(lastBracketIndex);
            } else state.codeEnvBuffer = '';
            break;
        }
    }
    return {
        hasToolCallUpdate,
        streamingToolCallUpdates
    };
}
function hasPartialTagAtEnd(buffer, expectedTags) {
    const lastBracketIndex = buffer.lastIndexOf('<');
    if (-1 === lastBracketIndex) return false;
    const afterBracket = buffer.substring(lastBracketIndex);
    if (afterBracket.includes('>')) return false;
    return expectedTags.some((tag)=>tag.startsWith(afterBracket) || afterBracket.startsWith(tag.substring(0, afterBracket.length)));
}
function generateToolCallId() {
    if (process.env.TEST) return 'random_id';
    return `call_${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
}
exports.extractCodeEnv = __webpack_exports__.extractCodeEnv;
for(var __webpack_i__ in __webpack_exports__)if (-1 === [
    "extractCodeEnv"
].indexOf(__webpack_i__)) exports[__webpack_i__] = __webpack_exports__[__webpack_i__];
Object.defineProperty(exports, '__esModule', {
    value: true
});
