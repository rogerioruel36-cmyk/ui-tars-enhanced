/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
function extractThink(content, state) {
    if (!state.thinkBuffer) state.thinkBuffer = '';
    if (state.thinkParseCompleted) return '';
    state.thinkBuffer += content;
    let result = '';
    while(state.thinkBuffer.length > 0)if (state.insideThink) {
        const closeMatch = state.thinkBuffer.indexOf('</think>');
        if (closeMatch > -1) {
            const thinkContent = state.thinkBuffer.substring(0, closeMatch);
            result += thinkContent;
            state.reasoningBuffer += thinkContent;
            state.thinkBuffer = state.thinkBuffer.substring(closeMatch + 8);
            state.insideThink = false;
            state.thinkParseCompleted = true;
            continue;
        }
        {
            const patterns = [
                '<',
                '</',
                '</t',
                '</th',
                '</thi',
                '</thin',
                '</think'
            ];
            let foundPartial = false;
            for (const pattern of patterns)if (state.thinkBuffer.endsWith(pattern)) {
                const beforePartial = state.thinkBuffer.substring(0, state.thinkBuffer.length - pattern.length);
                if (beforePartial) {
                    result += beforePartial;
                    state.reasoningBuffer += beforePartial;
                }
                state.thinkBuffer = pattern;
                foundPartial = true;
                break;
            }
            if (!foundPartial) {
                result += state.thinkBuffer;
                state.reasoningBuffer += state.thinkBuffer;
                state.thinkBuffer = '';
            }
            break;
        }
    } else {
        const openMatch = state.thinkBuffer.indexOf('<think>');
        if (-1 !== openMatch) {
            state.thinkBuffer = state.thinkBuffer.substring(openMatch + 7);
            state.insideThink = true;
            continue;
        }
        {
            const patterns = [
                '<',
                '<t',
                '<th',
                '<thi',
                '<thin',
                '<think'
            ];
            let foundPartial = false;
            for (const pattern of patterns)if (state.thinkBuffer.endsWith(pattern)) {
                state.thinkBuffer = pattern;
                foundPartial = true;
                break;
            }
            if (!foundPartial) state.thinkBuffer = '';
            break;
        }
    }
    return result;
}
function extractAnswer(content, state) {
    if (!state.answerBuffer) state.answerBuffer = '';
    state.answerBuffer += content;
    let result = '';
    while(state.answerBuffer.length > 0)if (state.insideAnswer) {
        const closeMatch = state.answerBuffer.indexOf('</answer>');
        if (-1 !== closeMatch) {
            const answerContent = state.answerBuffer.substring(0, closeMatch);
            result += answerContent;
            state.accumulatedAnswerBuffer += answerContent;
            state.answerBuffer = state.answerBuffer.substring(closeMatch + 9);
            state.insideAnswer = false;
            continue;
        }
        {
            const patterns = [
                '<',
                '</',
                '</a',
                '</an',
                '</ans',
                '</answ',
                '</answe',
                '</answer'
            ];
            let foundPartial = false;
            for (const pattern of patterns)if (state.answerBuffer.endsWith(pattern)) {
                const beforePartial = state.answerBuffer.substring(0, state.answerBuffer.length - pattern.length);
                if (beforePartial) {
                    result += beforePartial;
                    state.accumulatedAnswerBuffer += beforePartial;
                }
                state.answerBuffer = pattern;
                foundPartial = true;
                break;
            }
            if (!foundPartial) {
                result += state.answerBuffer;
                state.accumulatedAnswerBuffer += state.answerBuffer;
                state.answerBuffer = '';
            }
            break;
        }
    } else {
        const openMatch = state.answerBuffer.indexOf('<answer>');
        if (-1 !== openMatch) {
            state.answerBuffer = state.answerBuffer.substring(openMatch + 8);
            state.insideAnswer = true;
            continue;
        }
        {
            const patterns = [
                '<',
                '<a',
                '<an',
                '<ans',
                '<answ',
                '<answe',
                '<answer'
            ];
            let foundPartial = false;
            for (const pattern of patterns)if (state.answerBuffer.endsWith(pattern)) {
                state.answerBuffer = pattern;
                foundPartial = true;
                break;
            }
            if (!foundPartial) state.answerBuffer = '';
            break;
        }
    }
    return result;
}
export { extractAnswer, extractThink };
