/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    extractThinkT5: ()=>extractThinkT5
});
const prompt_t5_js_namespaceObject = require("../../environments/prompt_t5.js");
function generatePartialPatterns(tag) {
    const patterns = [];
    const openTag = `<${tag}`;
    for(let i = 1; i <= openTag.length; i++)patterns.push(openTag.substring(0, i));
    return patterns;
}
function generatePartialClosingPatterns(tag) {
    const patterns = [];
    const closeTag = `</${tag}`;
    const basicPatterns = [
        '<',
        '</',
        '</t',
        '</th',
        '</thi',
        '</thin',
        '</think'
    ];
    patterns.push(...basicPatterns);
    for(let i = 1; i <= closeTag.length; i++){
        const partial = closeTag.substring(0, i);
        if (!patterns.includes(partial)) patterns.push(partial);
    }
    return patterns;
}
function extractThinkT5(content, state) {
    if (!state.thinkBuffer) state.thinkBuffer = '';
    if (state.thinkParseCompleted) return '';
    state.thinkBuffer += content;
    let result = '';
    const T5_THINK_TAG = prompt_t5_js_namespaceObject.think_token;
    const THINK_OPEN_TAG = `<${T5_THINK_TAG}>`;
    const THINK_CLOSE_TAG = `</${T5_THINK_TAG}>`;
    while(state.thinkBuffer.length > 0)if (state.insideThink) {
        const closeMatch = state.thinkBuffer.indexOf(THINK_CLOSE_TAG);
        if (closeMatch > -1) {
            const thinkContent = state.thinkBuffer.substring(0, closeMatch);
            result += thinkContent;
            state.reasoningBuffer += thinkContent;
            state.thinkBuffer = state.thinkBuffer.substring(closeMatch + THINK_CLOSE_TAG.length);
            state.insideThink = false;
            state.thinkParseCompleted = true;
            continue;
        }
        {
            const patterns = generatePartialClosingPatterns(T5_THINK_TAG);
            let foundPartial = false;
            for (const pattern of patterns)if (state.thinkBuffer.endsWith(pattern)) {
                const beforePartial = state.thinkBuffer.substring(0, state.thinkBuffer.length - pattern.length);
                if (beforePartial) {
                    result += beforePartial;
                    state.reasoningBuffer += beforePartial;
                }
                state.thinkBuffer = pattern;
                foundPartial = true;
                break;
            }
            for(let i = 1; i <= Math.min(THINK_CLOSE_TAG.length - 1, state.thinkBuffer.length); i++){
                const partial = THINK_CLOSE_TAG.substring(0, i);
                if (state.thinkBuffer.endsWith(partial)) {
                    const beforePartial = state.thinkBuffer.substring(0, state.thinkBuffer.length - partial.length);
                    if (beforePartial) {
                        result += beforePartial;
                        state.reasoningBuffer += beforePartial;
                    }
                    state.thinkBuffer = partial;
                    foundPartial = true;
                    break;
                }
            }
            if (!foundPartial) {
                result += state.thinkBuffer;
                state.reasoningBuffer += state.thinkBuffer;
                state.thinkBuffer = '';
            }
            break;
        }
    } else {
        const openMatch = state.thinkBuffer.indexOf(THINK_OPEN_TAG);
        if (-1 !== openMatch) {
            state.thinkBuffer = state.thinkBuffer.substring(openMatch + THINK_OPEN_TAG.length);
            state.insideThink = true;
            continue;
        }
        {
            const patterns = generatePartialPatterns(T5_THINK_TAG);
            let foundPartial = false;
            for (const pattern of patterns)if (state.thinkBuffer.endsWith(pattern)) {
                state.thinkBuffer = pattern;
                foundPartial = true;
                break;
            }
            for(let i = 1; i <= Math.min(THINK_OPEN_TAG.length - 1, state.thinkBuffer.length); i++){
                const partial = THINK_OPEN_TAG.substring(0, i);
                if (state.thinkBuffer.endsWith(partial)) {
                    state.thinkBuffer = partial;
                    foundPartial = true;
                    break;
                }
            }
            if (!foundPartial) state.thinkBuffer = '';
            break;
        }
    }
    return result;
}
exports.extractThinkT5 = __webpack_exports__.extractThinkT5;
for(var __webpack_i__ in __webpack_exports__)if (-1 === [
    "extractThinkT5"
].indexOf(__webpack_i__)) exports[__webpack_i__] = __webpack_exports__[__webpack_i__];
Object.defineProperty(exports, '__esModule', {
    value: true
});
