import { ToolCallEngine, ToolCallEnginePrepareRequestContext, StreamProcessingState, StreamChunkResult, ParsedModelResponse, Tool } from '@tarko/agent';
import { ChatCompletionCreateParams, ChatCompletionChunk, ChatCompletionAssistantMessageParam, ChatCompletionMessageParam, MultimodalToolCallResult, AgentEventStream } from '@tarko/agent-interface';
import { ToolCallEngineCompositionConfig } from './types';
/**
 * Composable Tool Call Engine that orchestrates multiple tool call engines
 */
export declare class ComposableToolCallEngine extends ToolCallEngine {
    private logger;
    private engines;
    private defaultEngineProvider;
    private activeEngine?;
    constructor(config: ToolCallEngineCompositionConfig);
    /**
     * Select the appropriate engine based on context
     */
    private selectEngine;
    preparePrompt(instructions: string, tools: Tool[]): string | string[];
    prepareRequest(context: ToolCallEnginePrepareRequestContext): ChatCompletionCreateParams;
    initStreamProcessingState(): StreamProcessingState;
    processStreamingChunk(chunk: ChatCompletionChunk, state: StreamProcessingState): StreamChunkResult;
    finalizeStreamProcessing(state: StreamProcessingState): ParsedModelResponse;
    buildHistoricalAssistantMessage(currentLoopAssistantEvent: AgentEventStream.AssistantMessageEvent): ChatCompletionAssistantMessageParam;
    buildHistoricalToolCallResultMessages(toolCallResults: MultimodalToolCallResult[]): ChatCompletionMessageParam[];
    /**
     * Get information about available engines
     */
    getEngineInfo(): Array<{
        name: string;
        priority: number;
        description?: string;
    }>;
    /**
     * Get the currently active engine name
     */
    getActiveEngineName(): string | undefined;
}
//# sourceMappingURL=ComposableToolCallEngine.d.ts.map