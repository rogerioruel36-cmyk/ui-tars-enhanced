import { Agent, AgentOptions, LLMRequestHookPayload, LLMResponseHookPayload } from '@tarko/agent';
import { AgentPlugin } from './AgentPlugin';
export interface ComposableAgentOptions extends AgentOptions {
    /** Agent plugins to compose */
    plugins: AgentPlugin[];
}
/**
 * Main composable agent that orchestrates multiple agent plugins
 */
export declare class ComposableAgent extends Agent {
    private composer;
    constructor(options: ComposableAgentOptions);
    initialize(): Promise<void>;
    onLLMRequest(id: string, payload: LLMRequestHookPayload): Promise<void>;
    onLLMResponse(id: string, payload: LLMResponseHookPayload): Promise<void>;
    onEachAgentLoopStart(): Promise<void>;
    onEachAgentLoopEnd(): Promise<void>;
    onAgentLoopEnd(id: string): Promise<void>;
    onAfterToolCall(id: string, toolCall: {
        toolCallId: string;
        name: string;
    }, result: unknown): Promise<any>;
}
//# sourceMappingURL=ComposableAgent.d.ts.map