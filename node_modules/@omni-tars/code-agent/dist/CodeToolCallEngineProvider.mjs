/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { ToolCallEngineProvider } from "@omni-tars/core";
import { CodeToolCallEngine } from "./CodeToolCallEngine.mjs";
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class CodeToolCallEngineProvider extends ToolCallEngineProvider {
    createEngine() {
        return new CodeToolCallEngine();
    }
    canHandle(context) {
        if (context.toolCalls) {
            var _context_toolCalls;
            const codeToolNames = [
                'str_replace_editor',
                'execute_bash'
            ];
            const hasCodeTools = null == context ? void 0 : null == (_context_toolCalls = context.toolCalls) ? void 0 : _context_toolCalls.some((tool)=>codeToolNames.some((codeName)=>tool.function.name.toLowerCase().includes(codeName.toLowerCase())));
            return !!hasCodeTools;
        }
        if (context.latestAssistantMessage) {
            const hasCodeEnvTags = context.latestAssistantMessage.includes('<code_env>');
            if (hasCodeEnvTags) return true;
        }
        return false;
    }
    constructor(...args){
        super(...args), _define_property(this, "name", 'code-tool-call-engine'), _define_property(this, "priority", 80), _define_property(this, "description", 'Tool call engine optimized for code execution, file editing, and development tasks');
    }
}
export { CodeToolCallEngineProvider };
