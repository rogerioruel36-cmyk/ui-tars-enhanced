/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { ToolCallEngineProvider } from "@omni-tars/core";
import { McpToolCallEngine } from "./McpToolCallEngine.mjs";
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class McpToolCallEngineProvider extends ToolCallEngineProvider {
    createEngine() {
        return new McpToolCallEngine();
    }
    canHandle(context) {
        if (context.toolCalls) {
            var _context_toolCalls;
            const mcpToolNames = [
                'Search',
                'LinkReader'
            ];
            const hasMcpTools = null == context ? void 0 : null == (_context_toolCalls = context.toolCalls) ? void 0 : _context_toolCalls.some((tool)=>mcpToolNames.some((mcpName)=>tool.function.name.toLowerCase().includes(mcpName.toLowerCase())));
            return !!hasMcpTools;
        }
        if (context.latestAssistantMessage) {
            const hasMcpEnvTags = context.latestAssistantMessage.includes('<mcp_env>');
            if (hasMcpEnvTags) return true;
        }
        return false;
    }
    constructor(...args){
        super(...args), _define_property(this, "name", 'mcp-tool-call-engine'), _define_property(this, "priority", 100), _define_property(this, "description", 'Tool call engine optimized for MCP (Model Context Protocol) tasks with custom prompt parsing');
    }
}
export { McpToolCallEngineProvider };
