/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { AgentPlugin, MCP_ENVIRONMENT } from "@omni-tars/core";
import { SearchToolProvider } from "./tools/search.mjs";
import { LinkReaderToolProvider } from "./tools/linkReader.mjs";
import { McpManager } from "./tools/mcp.mjs";
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class McpAgentPlugin extends AgentPlugin {
    async initialize() {
        this.mcpManager.init();
        this.tools = [
            new SearchToolProvider(this.mcpManager).getTool(),
            new LinkReaderToolProvider(this.mcpManager).getTool()
        ];
    }
    constructor(option){
        super(), _define_property(this, "name", 'mcp-agent-plugin'), _define_property(this, "environmentSection", MCP_ENVIRONMENT), _define_property(this, "mcpManager", void 0);
        this.mcpManager = new McpManager({
            mcpServers: option.mcpServers.filter((s)=>s.enable)
        });
    }
}
export { McpAgentPlugin };
