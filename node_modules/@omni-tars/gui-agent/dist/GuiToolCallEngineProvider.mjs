/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { ToolCallEngineProvider } from "@omni-tars/core";
import { GUIAgentToolCallEngine } from "./GUIAgentToolCallEngine.mjs";
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class GuiToolCallEngineProvider extends ToolCallEngineProvider {
    createEngine() {
        return new GUIAgentToolCallEngine(this.agentMode);
    }
    canHandle(context) {
        if (context.toolCalls) {
            var _context_toolCalls;
            const toolNames = [
                'call_user',
                'click',
                'drag',
                'finished',
                'hotkey',
                'left_double',
                'mouse_down',
                'mouse_up',
                'move_to',
                'press',
                'release',
                'right_single',
                'scroll',
                'type',
                'wait'
            ];
            if ('game' !== this.agentMode.id) {
                toolNames.push('navigate');
                toolNames.push('navigate_back');
            }
            const hasGuiTools = null == context ? void 0 : null == (_context_toolCalls = context.toolCalls) ? void 0 : _context_toolCalls.some((tool)=>toolNames.some((guiName)=>tool.function.name.toLowerCase().includes(guiName.toLowerCase())));
            return !!hasGuiTools;
        }
        if (context.latestAssistantMessage) {
            const hasComputerEnvTags = context.latestAssistantMessage.includes('<computer_env>');
            if (hasComputerEnvTags) return true;
        }
        return false;
    }
    constructor(agentMode){
        super(), _define_property(this, "name", 'gui-tool-call-engine'), _define_property(this, "priority", 90), _define_property(this, "description", 'Tool call engine optimized for GUI automation, computer use, and visual interface interactions'), _define_property(this, "agentMode", void 0);
        this.agentMode = agentMode;
    }
}
export { GuiToolCallEngineProvider };
