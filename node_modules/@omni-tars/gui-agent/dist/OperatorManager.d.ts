import { Operator } from '@gui-agent/shared/base';
import { AgentMode } from '@omni-tars/core';
export declare class OperatorManager {
    private agentMode;
    private aioClient;
    private remoteOperator;
    private remoteBrowser;
    private operator;
    private browser;
    private sandboxUrl;
    private initialized;
    constructor(agentMode: AgentMode, sandboxUrl?: string);
    init(): Promise<void>;
    getInstance(): Promise<Operator | null>;
    getMode(): AgentMode;
    static create(agentMode?: AgentMode, sandboxUrl?: string): OperatorManager;
}
//# sourceMappingURL=OperatorManager.d.ts.map