import { Logger } from '@agent-infra/logger';
import type { Page } from 'puppeteer-core';
import type { OSType, BrowserType } from '../type';
export interface HotkeyOptions {
    delay: number;
}
export declare class Hotkey {
    private osName;
    private browserName;
    private logger;
    constructor(envInfo: {
        osName: OSType;
        browserName: BrowserType;
    }, logger?: Logger);
    /**
     * Format the hotkey string into an array of KeyInput.
     *
     * - example1: 'ctrl+c' -> ['Control', 'C']
     * - example2: 'control+c' -> ['Control', 'C']
     * - example3: 'Control+C' -> ['Control', 'C']
     */
    private formatHotkey;
    /**
     * adapt for common macOS system and chrome hotkeys
     *
     * see issues: https://github.com/bytedance/UI-TARS-desktop/pull/560
     *
     * example: 'Ctrl+C' -> CDP: { key: 'KeyC', commands: 'Copy' }
     */
    private macOSCDPHotKey;
    press(page: Page, hotkey: string, options?: Readonly<HotkeyOptions>): Promise<void>;
    down(page: Page, hotkey: string, options?: Readonly<HotkeyOptions>): Promise<void>;
    up(page: Page, hotkey: string, options?: Readonly<HotkeyOptions>): Promise<void>;
}
//# sourceMappingURL=index.d.ts.map