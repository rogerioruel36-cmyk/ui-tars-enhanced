import { Logger } from "@agent-infra/logger";
import type { ClientConfig, ApiResponse, AIORequestOption } from "./types";
export declare class BaseClient {
    protected baseUrl: string;
    protected timeout: number;
    protected retries: number;
    protected retryDelay: number;
    protected logger: Logger;
    constructor(config: ClientConfig);
    healthCheck(): Promise<{
        msg: string;
    }>;
    private abortCallback;
    protected sleep(ms: number): Promise<void>;
    protected fetchWithTimeout(url: string, options: AIORequestOption): Promise<Response>;
    protected request<T>(path: string, options: AIORequestOption & {
        raw: true;
    }): Promise<Response>;
    protected request<T>(path: string, options?: AIORequestOption): Promise<ApiResponse<T>>;
}
//# sourceMappingURL=base.d.ts.map