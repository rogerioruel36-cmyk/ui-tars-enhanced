/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import { ConsoleLogger, LogLevel } from "@agent-infra/logger";
import { AIOHybridOperator } from "./AIOHybridOperator.mjs";
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
const defaultLogger = new ConsoleLogger(void 0, LogLevel.DEBUG);
class AIOGameOperator extends AIOHybridOperator {
    supportedActions() {
        return [
            'call_user',
            'finished',
            'wait',
            'mouse_down',
            'mouse_up',
            'mouse_move',
            'click',
            'double_click',
            'right_click',
            'middle_click',
            'drag',
            'type',
            'hotkey',
            'press',
            'release',
            'scroll'
        ];
    }
    async initialize() {
        var _this_aioBrowser;
        await super.initialize();
        if (!this.targetUrl) return void this.logger.warn('targetUrl is null');
        await (null == (_this_aioBrowser = this.aioBrowser) ? void 0 : _this_aioBrowser.handleNavigate({
            url: this.targetUrl
        }));
        this.logger.info(`initialize: goto ${this.targetUrl} successfully`);
    }
    constructor(options, logger = defaultLogger){
        const operatorLogger = logger.spawn('[Game]');
        super(options, operatorLogger), _define_property(this, "name", 'aio-game'), _define_property(this, "description", 'Operator for game environment'), _define_property(this, "targetUrl", void 0);
        this.targetUrl = options.targetUrl;
    }
}
export { AIOGameOperator };

//# sourceMappingURL=AIOGameOperator.mjs.map