import { BaseAction, ExecuteOutput, ExecuteParams, ScreenshotOutput, SupportedActionType } from '@gui-agent/shared/types';
import { Operator, ScreenContext } from '@gui-agent/shared/base';
import { ConsoleLogger } from '@agent-infra/logger';
import { AIOComputer } from './AIOComputer';
import { AIOBrowser } from './AIOBrowser';
import type { AIOHybridOptions } from './types';
export declare class AIOHybridOperator extends Operator {
    private options;
    protected logger: ConsoleLogger;
    protected aioBrowser: AIOBrowser | null;
    protected aioComputer: AIOComputer;
    private screenshotWidth;
    private screenshotHeight;
    constructor(options: AIOHybridOptions, logger?: ConsoleLogger);
    protected initialize(): Promise<void>;
    protected supportedActions(): Array<SupportedActionType>;
    protected screenContext(): ScreenContext;
    screenshot(): Promise<ScreenshotOutput>;
    protected execute(params: ExecuteParams): Promise<ExecuteOutput>;
    singleActionExecutor(action: BaseAction): Promise<ExecuteOutput>;
    private getCurrentUrl;
    private calculateRealCoords;
    private handleClick;
    private handleDrag;
    private handleScroll;
}
//# sourceMappingURL=AIOHybridOperator.d.ts.map