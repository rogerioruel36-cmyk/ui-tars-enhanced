/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    PromptEngineeringToolCallEngine: ()=>PromptEngineeringToolCallEngine
});
const agent_interface_namespaceObject = require("@tarko/agent-interface");
const index_js_namespaceObject = require("../utils/index.js");
const shared_utils_namespaceObject = require("@tarko/shared-utils");
const external_utils_js_namespaceObject = require("./utils.js");
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class PromptEngineeringToolCallEngine extends agent_interface_namespaceObject.ToolCallEngine {
    preparePrompt(instructions, tools) {
        if (!tools || 0 === tools.length) return instructions;
        this.logger.info(`Preparing prompt with ${tools.length} tools`);
        const toolsDescription = tools.map((tool)=>{
            const schema = (0, index_js_namespaceObject.zodToJsonSchema)(tool.schema);
            return `## ${tool.name}

Description: ${tool.description}

Parameters JSON Schema:
\`\`\`json
${JSON.stringify(schema)}
\`\`\`

`;
        }).join('\n\n');
        return `${instructions}

<tool_instruction>
  1. You have access to the following tools:

  <available_tools>
  ${toolsDescription}
  </available_tools>

  2. To use a tool, your response MUST use the following format, you need to ensure that it is a valid JSON string matches the Parameters JSON Schema:

  <tool_call>
  {
    "name": "tool_name",
    "parameters": {
      "param1": "value1",
      "param2": "value2"
    }
  }
  </tool_call>

  3. If you want to provide a final answer without using tools, respond in a conversational manner WITHOUT using the tool_call format.
  4. WARNING:
    4.1. You can always ONLY call tools mentioned in <available_tools>
    4.2. After outputting </tool_call>, you MUST STOP immediately and wait for the tool result in the next agent loop. DO NOT generate any additional text.
    4.3. When you receive tool results, they will be provided in a user message. Use these results to continue your reasoning or provide a final answer.
</tool_instruction>
`;
    }
    prepareRequest(context) {
        const { model, messages, temperature = 0.7, top_p } = context;
        this.logger.debug(`Preparing request for model: ${model}`);
        return {
            model,
            messages,
            temperature,
            top_p,
            stream: false,
            stop: [
                '</tool_call>',
                '</tool_call>\n\n'
            ],
            stop_sequences: [
                '</tool_call>',
                '</tool_call>\n\n'
            ]
        };
    }
    initStreamProcessingState() {
        return {
            contentBuffer: '',
            toolCalls: [],
            reasoningBuffer: '',
            finishReason: null,
            currentToolCallBuffer: '',
            hasActiveToolCall: false,
            normalContentBuffer: '',
            parserState: 'normal',
            partialTagBuffer: '',
            toolNameExtracted: false,
            emittingParameters: false,
            currentToolName: '',
            currentToolCallId: '',
            parameterBracketDepth: 0,
            parameterContentStarted: false
        };
    }
    processStreamingChunk(chunk, state) {
        var _chunk_choices_, _chunk_choices_1;
        const extendedState = state;
        const delta = null == (_chunk_choices_ = chunk.choices[0]) ? void 0 : _chunk_choices_.delta;
        let content = '';
        let reasoningContent = '';
        let hasToolCallUpdate = false;
        const streamingToolCallUpdates = [];
        if (null == (_chunk_choices_1 = chunk.choices[0]) ? void 0 : _chunk_choices_1.finish_reason) extendedState.finishReason = chunk.choices[0].finish_reason;
        if (null == delta ? void 0 : delta.reasoning_content) {
            reasoningContent = delta.reasoning_content;
            extendedState.reasoningBuffer += reasoningContent;
        }
        if (null == delta ? void 0 : delta.content) {
            const newContent = delta.content;
            extendedState.contentBuffer += newContent;
            const result = this.processContentWithStateMachine(newContent, extendedState);
            content = result.content;
            hasToolCallUpdate = result.hasToolCallUpdate;
            if (result.streamingToolCallUpdates) streamingToolCallUpdates.push(...result.streamingToolCallUpdates);
        }
        const mergedUpdates = this.mergeStreamingToolCallUpdates(streamingToolCallUpdates);
        return {
            content,
            reasoningContent,
            hasToolCallUpdate,
            toolCalls: extendedState.toolCalls,
            streamingToolCallUpdates: mergedUpdates.length > 0 ? mergedUpdates : void 0
        };
    }
    mergeStreamingToolCallUpdates(updates) {
        if (updates.length <= 1) return updates;
        const merged = [];
        let currentUpdate = null;
        for (const update of updates)if (!currentUpdate || currentUpdate.toolCallId !== update.toolCallId || currentUpdate.toolName !== update.toolName || currentUpdate.isComplete || update.isComplete) {
            if (currentUpdate) merged.push(currentUpdate);
            currentUpdate = {
                ...update
            };
        } else currentUpdate.argumentsDelta += update.argumentsDelta;
        if (currentUpdate) merged.push(currentUpdate);
        return merged;
    }
    processContentWithStateMachine(newContent, state) {
        let outputContent = '';
        let hasToolCallUpdate = false;
        const streamingToolCallUpdates = [];
        for(let i = 0; i < newContent.length; i++){
            const char = newContent[i];
            switch(state.parserState){
                case 'normal':
                    if ('<' === char) {
                        state.parserState = 'possible_tag_start';
                        state.partialTagBuffer = '<';
                    } else {
                        state.normalContentBuffer += char;
                        outputContent += char;
                    }
                    break;
                case 'possible_tag_start':
                    state.partialTagBuffer += char;
                    if ('<tool_call>' === state.partialTagBuffer) {
                        state.parserState = 'collecting_tool_call';
                        state.hasActiveToolCall = true;
                        state.currentToolCallBuffer = '';
                        state.partialTagBuffer = '';
                        state.toolNameExtracted = false;
                        state.emittingParameters = false;
                        state.currentToolName = '';
                        state.currentToolCallId = '';
                        state.parameterBracketDepth = 0;
                        state.parameterContentStarted = false;
                    } else if (!this.isPossibleTagStart(state.partialTagBuffer)) {
                        state.normalContentBuffer += state.partialTagBuffer;
                        outputContent += state.partialTagBuffer;
                        state.parserState = 'normal';
                        state.partialTagBuffer = '';
                    }
                    break;
                case 'collecting_tool_call':
                    if ('<' === char) {
                        state.parserState = 'possible_tag_end';
                        state.partialTagBuffer = '<';
                    } else {
                        state.currentToolCallBuffer += char;
                        if (!state.toolNameExtracted) {
                            const toolName = this.tryExtractToolName(state.currentToolCallBuffer);
                            if (toolName) {
                                state.toolNameExtracted = true;
                                state.currentToolName = toolName;
                                state.currentToolCallId = this.generateToolCallId();
                                streamingToolCallUpdates.push({
                                    toolCallId: state.currentToolCallId,
                                    toolName: state.currentToolName,
                                    argumentsDelta: '',
                                    isComplete: false
                                });
                                hasToolCallUpdate = true;
                            }
                        }
                        if (state.toolNameExtracted) {
                            this.handleParameterExtraction(char, state, streamingToolCallUpdates);
                            if (streamingToolCallUpdates.length > 0) hasToolCallUpdate = true;
                        }
                    }
                    break;
                case 'possible_tag_end':
                    state.partialTagBuffer += char;
                    if ('</tool_call>' === state.partialTagBuffer) {
                        const toolCallUpdate = this.completeToolCall(state);
                        if (toolCallUpdate) {
                            hasToolCallUpdate = true;
                            streamingToolCallUpdates.push(toolCallUpdate);
                        }
                        state.parserState = 'normal';
                        state.hasActiveToolCall = false;
                        state.currentToolCallBuffer = '';
                        state.partialTagBuffer = '';
                        state.toolNameExtracted = false;
                        state.emittingParameters = false;
                        state.currentToolName = '';
                        state.currentToolCallId = '';
                        state.parameterBracketDepth = 0;
                        state.parameterContentStarted = false;
                    } else if (!this.isPossibleTagEnd(state.partialTagBuffer)) {
                        state.currentToolCallBuffer += state.partialTagBuffer;
                        state.parserState = 'collecting_tool_call';
                        if (state.toolNameExtracted) {
                            for (const bufferedChar of state.partialTagBuffer)this.handleParameterExtraction(bufferedChar, state, streamingToolCallUpdates);
                            if (streamingToolCallUpdates.length > 0) hasToolCallUpdate = true;
                        }
                        state.partialTagBuffer = '';
                    }
                    break;
            }
        }
        return {
            content: outputContent,
            hasToolCallUpdate,
            streamingToolCallUpdates: streamingToolCallUpdates.length > 0 ? streamingToolCallUpdates : void 0
        };
    }
    handleParameterExtraction(char, state, streamingToolCallUpdates) {
        if (!state.emittingParameters) {
            const parametersStart = this.findParametersStart(state.currentToolCallBuffer);
            if (-1 !== parametersStart) {
                state.emittingParameters = true;
                state.parameterContentStarted = false;
                state.parameterBracketDepth = 0;
            }
        }
        if (state.emittingParameters) {
            if ('{' === char) if (state.parameterContentStarted) {
                state.parameterBracketDepth++;
                streamingToolCallUpdates.push({
                    toolCallId: state.currentToolCallId,
                    toolName: state.currentToolName,
                    argumentsDelta: char,
                    isComplete: false
                });
            } else {
                state.parameterContentStarted = true;
                state.parameterBracketDepth = 1;
                streamingToolCallUpdates.push({
                    toolCallId: state.currentToolCallId,
                    toolName: state.currentToolName,
                    argumentsDelta: char,
                    isComplete: false
                });
            }
            else if ('}' === char) {
                if (state.parameterContentStarted && state.parameterBracketDepth > 0) {
                    state.parameterBracketDepth--;
                    if (0 === state.parameterBracketDepth) {
                        streamingToolCallUpdates.push({
                            toolCallId: state.currentToolCallId,
                            toolName: state.currentToolName,
                            argumentsDelta: char,
                            isComplete: false
                        });
                        state.emittingParameters = false;
                    } else streamingToolCallUpdates.push({
                        toolCallId: state.currentToolCallId,
                        toolName: state.currentToolName,
                        argumentsDelta: char,
                        isComplete: false
                    });
                }
            } else if (state.parameterContentStarted) streamingToolCallUpdates.push({
                toolCallId: state.currentToolCallId,
                toolName: state.currentToolName,
                argumentsDelta: char,
                isComplete: false
            });
        }
    }
    tryExtractToolName(content) {
        try {
            const nameMatch = content.match(/"name"\s*:\s*"([^"]+)"/);
            if (nameMatch) return nameMatch[1];
        } catch (e) {}
        return null;
    }
    findParametersStart(content) {
        const parametersMatch = content.match(/"parameters"\s*:\s*\{/);
        if (parametersMatch && void 0 !== parametersMatch.index) return parametersMatch.index + parametersMatch[0].length - 1;
        return -1;
    }
    generateToolCallId() {
        return (0, shared_utils_namespaceObject.isTest)() ? "call_1747633091730_6m2magifs" : `call_${Date.now()}_${Math.random().toString(36).substring(2, 11)}`;
    }
    isPossibleTagStart(buffer) {
        const target = '<tool_call>';
        return target.startsWith(buffer);
    }
    isPossibleTagEnd(buffer) {
        const target = '</tool_call>';
        return target.startsWith(buffer);
    }
    extractCleanJsonContent(content) {
        const trimmed = content.trim();
        const jsonMatch = trimmed.match(/^\s*\{[\s\S]*?\}(?=\s*(?:\}|\n|$))/);
        if (jsonMatch) {
            const candidate = jsonMatch[0].trim();
            try {
                JSON.parse(candidate);
                return candidate;
            } catch  {}
        }
        return trimmed;
    }
    completeToolCall(state) {
        try {
            const toolCallContent = this.extractCleanJsonContent(state.currentToolCallBuffer);
            const toolCallData = JSON.parse(toolCallContent);
            if (toolCallData && toolCallData.name) {
                const toolCallId = state.currentToolCallId || this.generateToolCallId();
                const toolCall = {
                    id: toolCallId,
                    type: 'function',
                    function: {
                        name: toolCallData.name,
                        arguments: JSON.stringify(toolCallData.parameters || {})
                    }
                };
                state.toolCalls.push(toolCall);
                this.logger.debug(`Completed tool call: ${toolCallData.name} with ID: ${toolCallId}`);
                return {
                    toolCallId,
                    toolName: toolCallData.name,
                    argumentsDelta: '',
                    isComplete: true
                };
            }
        } catch (error) {
            this.logger.error('Failed to parse tool call JSON:', error);
        }
        return null;
    }
    finalizeStreamProcessing(state) {
        const extendedState = state;
        let finalContent = extendedState.normalContentBuffer;
        let finalToolCalls = [
            ...extendedState.toolCalls
        ];
        if (extendedState.hasActiveToolCall && extendedState.currentToolCallBuffer) {
            this.logger.info('Detected incomplete tool call due to stop_sequence, attempting to complete parsing');
            try {
                let toolCallContent = this.extractCleanJsonContent(extendedState.currentToolCallBuffer);
                if (toolCallContent && !toolCallContent.endsWith('}')) {
                    const openBraces = (toolCallContent.match(/\{/g) || []).length;
                    const closeBraces = (toolCallContent.match(/\}/g) || []).length;
                    const missingBraces = openBraces - closeBraces;
                    if (missingBraces > 0) {
                        toolCallContent += '}'.repeat(missingBraces);
                        this.logger.debug(`Added ${missingBraces} closing braces to complete JSON`);
                    }
                }
                const toolCallData = JSON.parse(toolCallContent);
                if (toolCallData && toolCallData.name) {
                    const toolCallId = extendedState.currentToolCallId || this.generateToolCallId();
                    const toolCall = {
                        id: toolCallId,
                        type: 'function',
                        function: {
                            name: toolCallData.name,
                            arguments: JSON.stringify(toolCallData.parameters || {})
                        }
                    };
                    finalToolCalls.push(toolCall);
                    this.logger.info(`Successfully recovered tool call: ${toolCallData.name} from truncated content`);
                }
            } catch (error) {
                this.logger.warn(`Failed to parse incomplete tool call: ${error}`);
            }
        }
        if (0 === finalToolCalls.length && this.hasCompletedToolCall(extendedState.contentBuffer)) {
            const { cleanedContent, extractedToolCalls } = this.extractToolCalls(extendedState.contentBuffer);
            finalContent = cleanedContent;
            finalToolCalls = extractedToolCalls;
        }
        const finishReason = finalToolCalls.length > 0 ? 'tool_calls' : extendedState.finishReason || 'stop';
        this.logger.info(`Finalized with ${finalToolCalls.length} tool calls, finish_reason: ${finishReason}`);
        return {
            content: finalContent,
            rawContent: extendedState.contentBuffer,
            reasoningContent: extendedState.reasoningBuffer || void 0,
            toolCalls: finalToolCalls.length > 0 ? finalToolCalls : void 0,
            finishReason
        };
    }
    hasCompletedToolCall(content) {
        return content.includes('<tool_call>') && content.includes('</tool_call>');
    }
    extractToolCalls(content) {
        const toolCalls = [];
        const toolCallRegex = /<tool_call>([\s\S]*?)<\/tool_call>/g;
        let match;
        let cleanedContent = content;
        while(null !== (match = toolCallRegex.exec(content))){
            const toolCallContent = this.extractCleanJsonContent(match[1]);
            try {
                const toolCallData = JSON.parse(toolCallContent);
                if (toolCallData && toolCallData.name) {
                    const toolCallId = this.generateToolCallId();
                    toolCalls.push({
                        id: toolCallId,
                        type: 'function',
                        function: {
                            name: toolCallData.name,
                            arguments: JSON.stringify(toolCallData.parameters || {})
                        }
                    });
                    this.logger.debug(`Found tool call: ${toolCallData.name} with ID: ${toolCallId}`);
                }
            } catch (error) {
                this.logger.error('Failed to parse tool call JSON:', error);
            }
        }
        cleanedContent = content.replace(/<tool_call>[\s\S]*?<\/tool_call>/g, '').trim();
        return {
            cleanedContent,
            extractedToolCalls: toolCalls
        };
    }
    buildHistoricalAssistantMessage(currentLoopAssistantEvent) {
        const { rawContent } = currentLoopAssistantEvent;
        return {
            role: 'assistant',
            content: rawContent
        };
    }
    buildHistoricalToolCallResultMessages(toolCallResults) {
        return (0, external_utils_js_namespaceObject.buildToolCallResultMessages)(toolCallResults, false);
    }
    constructor(...args){
        super(...args), _define_property(this, "logger", (0, shared_utils_namespaceObject.getLogger)('PromptEngine'));
    }
}
exports.PromptEngineeringToolCallEngine = __webpack_exports__.PromptEngineeringToolCallEngine;
for(var __webpack_i__ in __webpack_exports__)if (-1 === [
    "PromptEngineeringToolCallEngine"
].indexOf(__webpack_i__)) exports[__webpack_i__] = __webpack_exports__[__webpack_i__];
Object.defineProperty(exports, '__esModule', {
    value: true
});

//# sourceMappingURL=PromptEngineeringToolCallEngine.js.map