import { Tool, ToolCallEngine, ParsedModelResponse, ToolCallEnginePrepareRequestContext, AgentEventStream, MultimodalToolCallResult, ChatCompletionChunk, ChatCompletionMessageParam, ChatCompletionCreateParams, ChatCompletionAssistantMessageParam, StreamProcessingState, StreamChunkResult } from '@tarko/agent-interface';
/**
 * A Tool Call Engine based on native Function Call.
 */
export declare class NativeToolCallEngine extends ToolCallEngine {
    private logger;
    preparePrompt(instructions: string, tools: Tool[]): string;
    prepareRequest(context: ToolCallEnginePrepareRequestContext): ChatCompletionCreateParams;
    /**
     * Initialize stream processing state for native tool calls
     */
    initStreamProcessingState(): StreamProcessingState;
    /**
     * Process a streaming chunk for native tool calls
     * For native engines, we can directly use the tool_calls property
     */
    processStreamingChunk(chunk: ChatCompletionChunk, state: StreamProcessingState): StreamChunkResult;
    /**
     * Process tool calls data from a chunk
     */
    private processToolCallsInChunk;
    /**
     * Finalize the stream processing and extract the final response
     */
    finalizeStreamProcessing(state: StreamProcessingState): ParsedModelResponse;
    buildHistoricalAssistantMessage(currentLoopAssistantEvent: AgentEventStream.AssistantMessageEvent): ChatCompletionAssistantMessageParam;
    buildHistoricalToolCallResultMessages(toolCallResults: MultimodalToolCallResult[]): ChatCompletionMessageParam[];
}
//# sourceMappingURL=NativeToolCallEngine.d.ts.map