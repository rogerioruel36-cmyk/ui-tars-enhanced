import { Tool as MCPTool } from '@modelcontextprotocol/sdk/types.js';
import { IMCPClient, MCPClientResult, MCPServerConfig } from './mcp-types';
import type { Logger } from '@agent-infra/logger';
export declare class MCPClient implements IMCPClient {
    private serverName;
    private config;
    private logger;
    private client;
    private transport;
    private tools;
    constructor(serverName: string, config: MCPServerConfig, logger: Logger);
    /**
     * Get enhanced PATH including common tool locations
     */
    private getEnhancedPath;
    initialize(): Promise<MCPTool[]>;
    callTool(toolName: string, args: any): Promise<MCPClientResult>;
    close(): Promise<void>;
    getTools(): MCPTool[];
}
//# sourceMappingURL=mcp-client.d.ts.map