import { Tool } from '@tarko/agent';
import { IMCPClient } from './mcp-types';
/**
 * Adapts MCP tools to our local Tool format
 */
export declare class MCPToolAdapter {
    private mcpClient;
    private serverName;
    constructor(mcpClient: IMCPClient, serverName: string);
    /**
     * Create Tool instances from MCP tools
     */
    createTools(): Tool<any>[];
}
//# sourceMappingURL=mcp-tool-adapter.d.ts.map