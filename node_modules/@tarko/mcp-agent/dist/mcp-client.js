/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    MCPClient: ()=>MCPClient
});
const index_js_namespaceObject = require("@modelcontextprotocol/sdk/client/index.js");
const stdio_js_namespaceObject = require("@modelcontextprotocol/sdk/client/stdio.js");
const sse_js_namespaceObject = require("@modelcontextprotocol/sdk/client/sse.js");
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class MCPClient {
    getEnhancedPath(originalPath) {
        const pathSeparator = 'win32' === process.platform ? ';' : ':';
        const existingPaths = new Set(originalPath.split(pathSeparator).filter(Boolean));
        const homeDir = process.env.HOME || process.env.USERPROFILE || '';
        const newPaths = [];
        if ('darwin' === process.platform) newPaths.push('/bin', '/usr/bin', '/usr/local/bin', '/usr/local/sbin', '/opt/homebrew/bin', '/opt/homebrew/sbin', '/usr/local/opt/node/bin', `${homeDir}/.nvm/current/bin`, `${homeDir}/.npm-global/bin`, `${homeDir}/.yarn/bin`, `${homeDir}/.cargo/bin`, '/opt/local/bin');
        if ('linux' === process.platform) newPaths.push('/bin', '/usr/bin', '/usr/local/bin', `${homeDir}/.nvm/current/bin`, `${homeDir}/.npm-global/bin`, `${homeDir}/.yarn/bin`, `${homeDir}/.cargo/bin`, '/snap/bin');
        if ('win32' === process.platform) newPaths.push(`${process.env.APPDATA}\\npm`, `${homeDir}\\AppData\\Local\\Yarn\\bin`, `${homeDir}\\.cargo\\bin`);
        newPaths.forEach((path)=>{
            if (path && !existingPaths.has(path)) existingPaths.add(path);
        });
        return Array.from(existingPaths).join(pathSeparator);
    }
    async initialize() {
        if (this.config.command) {
            let cmd = this.config.command;
            if ('win32' === process.platform) {
                if ('npx' === cmd) cmd = `${cmd}.cmd`;
                else if ('node' === cmd) cmd = `${cmd}.exe`;
            }
            const mergedEnv = {
                PATH: this.getEnhancedPath(process.env.PATH || ''),
                ...this.config.env
            };
            this.logger.info(`Creating stdio transport for ${this.serverName} with command: ${cmd}`);
            this.transport = new stdio_js_namespaceObject.StdioClientTransport({
                command: cmd,
                args: this.config.args || [],
                stderr: 'win32' === process.platform ? 'pipe' : 'inherit',
                env: mergedEnv
            });
        } else if (this.config.url) {
            this.logger.info(`Creating SSE transport for ${this.serverName} with URL: ${this.config.url}`);
            this.transport = new sse_js_namespaceObject.SSEClientTransport(new URL(this.config.url));
        } else {
            this.logger.error(`Invalid MCP server configuration for: ${this.serverName}`);
            throw new Error(`Invalid MCP server configuration for: ${this.serverName}`);
        }
        this.logger.info(`Connecting to MCP server: ${this.serverName}`);
        await this.client.connect(this.transport);
        const response = await this.client.listTools();
        this.tools = response.tools;
        this.logger.success(`Connected to ${this.serverName}, found ${this.tools.length} tools`);
        return this.tools;
    }
    async callTool(toolName, args) {
        if (!this.client) {
            this.logger.error('MCP Client not initialized');
            throw new Error('MCP Client not initialized');
        }
        try {
            this.logger.info(`Calling tool: ${toolName}`);
            this.logger.debug("Tool args:", args);
            const result = await this.client.callTool({
                name: toolName,
                arguments: args
            });
            this.logger.debug(`Tool ${toolName} executed successfully`);
            return result;
        } catch (error) {
            this.logger.error(`Error calling MCP tool ${toolName}:`, error);
            return {
                content: `Error: Failed to execute tool ${toolName}: ${error}`
            };
        }
    }
    async close() {
        if (this.transport) {
            this.logger.info(`Closing transport for ${this.serverName}`);
            await this.transport.close();
            this.transport = null;
            this.logger.success(`Transport for ${this.serverName} closed successfully`);
        }
    }
    getTools() {
        return [
            ...this.tools
        ];
    }
    constructor(serverName, config, logger){
        _define_property(this, "serverName", void 0);
        _define_property(this, "config", void 0);
        _define_property(this, "logger", void 0);
        _define_property(this, "client", void 0);
        _define_property(this, "transport", void 0);
        _define_property(this, "tools", void 0);
        this.serverName = serverName;
        this.config = config;
        this.logger = logger;
        this.transport = null;
        this.tools = [];
        this.client = new index_js_namespaceObject.Client({
            name: `mcp-client-${serverName}`,
            version: '1.0.0'
        }, {
            capabilities: {
                prompts: {},
                resources: {},
                tools: {}
            }
        });
    }
}
exports.MCPClient = __webpack_exports__.MCPClient;
for(var __webpack_i__ in __webpack_exports__)if (-1 === [
    "MCPClient"
].indexOf(__webpack_i__)) exports[__webpack_i__] = __webpack_exports__[__webpack_i__];
Object.defineProperty(exports, '__esModule', {
    value: true
});

//# sourceMappingURL=mcp-client.js.map