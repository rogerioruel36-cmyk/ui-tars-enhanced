import { Tool } from '@modelcontextprotocol/sdk/types.js';
import { IMCPClient, MCPServerConfig } from './mcp-types';
import type { Logger } from '@agent-infra/logger';
/**
 * Implementation of IMCPClient using @agent-infra/mcp-client
 */
export declare class MCPClientV2 implements IMCPClient {
    private logger;
    private v2Client;
    private serverName;
    private tools;
    private isInitialized;
    constructor(serverName: string, config: MCPServerConfig, logger: Logger, defaultTimeout?: number);
    initialize(): Promise<Tool[]>;
    callTool(toolName: string, args: unknown): Promise<unknown>;
    close(): Promise<void>;
    getTools(): Tool[];
}
//# sourceMappingURL=mcp-client-v2.d.ts.map