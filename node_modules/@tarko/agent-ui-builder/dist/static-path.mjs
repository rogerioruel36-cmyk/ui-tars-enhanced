/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
import * as __WEBPACK_EXTERNAL_MODULE_fs__ from "fs";
import path from "path";
var __webpack_modules__ = {
    fs: function(module) {
        module.exports = __WEBPACK_EXTERNAL_MODULE_fs__;
    }
};
var __webpack_module_cache__ = {};
function __webpack_require__(moduleId) {
    var cachedModule = __webpack_module_cache__[moduleId];
    if (void 0 !== cachedModule) return cachedModule.exports;
    var module = __webpack_module_cache__[moduleId] = {
        exports: {}
    };
    __webpack_modules__[moduleId](module, module.exports, __webpack_require__);
    return module.exports;
}
function getDefaultStaticPath() {
    return path.resolve(__dirname, '../static');
}
function isDefaultStaticPathValid() {
    try {
        const fs = __webpack_require__("fs");
        const staticPath = getDefaultStaticPath();
        const indexPath = path.join(staticPath, 'index.html');
        return fs.existsSync(indexPath);
    } catch  {
        return false;
    }
}
function getStaticPath(customPath) {
    const fs = __webpack_require__("fs");
    if (customPath) {
        const indexPath = path.join(customPath, 'index.html');
        if (fs.existsSync(indexPath)) return customPath;
        throw new Error(`Custom static path invalid: ${customPath} (index.html not found)`);
    }
    const defaultPath = getDefaultStaticPath();
    const defaultIndexPath = path.join(defaultPath, 'index.html');
    if (fs.existsSync(defaultIndexPath)) return defaultPath;
    throw new Error(`No valid static path found. Default path ${defaultPath} does not contain index.html. Make sure agent-ui is built or provide a custom staticPath.`);
}
export { getDefaultStaticPath, getStaticPath, isDefaultStaticPathValid };

//# sourceMappingURL=static-path.mjs.map