/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
function isPlainObject(value) {
    return value && 'object' == typeof value && !Array.isArray(value);
}
function deepMerge(target, source, options = {
    nonDestructive: true
}) {
    if (!source) return options.nonDestructive ? {
        ...target
    } : target;
    const result = options.nonDestructive ? {
        ...target
    } : target;
    for(const key in source)if (Object.prototype.hasOwnProperty.call(source, key)) {
        const sourceValue = source[key];
        const targetValue = target[key];
        if (isPlainObject(sourceValue) && isPlainObject(targetValue)) result[key] = deepMerge(targetValue, sourceValue, options);
        else if (void 0 !== sourceValue) result[key] = sourceValue;
    }
    return result;
}
export { deepMerge };

//# sourceMappingURL=deepMerge.mjs.map