import { AgentEventStream, FileAgentStorageImplementation } from '@tarko/interface';
import { StorageProvider, SessionInfo } from './types';
/**
 * File-based storage provider using lowdb
 * Stores data in a JSON file for persistence
 * Suitable for local development and small-scale deployments
 */
export declare class FileStorageProvider implements StorageProvider {
    private db;
    private initialized;
    readonly dbPath: string;
    constructor(config: FileAgentStorageImplementation);
    initialize(): Promise<void>;
    createSession(sessionInfo: SessionInfo): Promise<SessionInfo>;
    updateSessionInfo(sessionId: string, sessionInfo: Partial<Omit<SessionInfo, 'id'>>): Promise<SessionInfo>;
    getSessionInfo(sessionId: string): Promise<SessionInfo | null>;
    getAllSessions(): Promise<SessionInfo[]>;
    deleteSession(sessionId: string): Promise<boolean>;
    saveEvent(sessionId: string, event: AgentEventStream.Event): Promise<void>;
    getSessionEvents(sessionId: string): Promise<AgentEventStream.Event[]>;
    close(): Promise<void>;
    private ensureInitialized;
}
//# sourceMappingURL=FileStorageProvider.d.ts.map