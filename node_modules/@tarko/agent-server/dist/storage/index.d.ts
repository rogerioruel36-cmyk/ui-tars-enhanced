import { StorageProvider } from './types';
import { AgentStorageImplementation } from '@tarko/interface';
export * from './types';
/**
 * Creates and returns a storage provider based on the options
 * @param options Storage configuration options
 */
export declare function createStorageProvider(options?: AgentStorageImplementation): StorageProvider;
//# sourceMappingURL=index.d.ts.map