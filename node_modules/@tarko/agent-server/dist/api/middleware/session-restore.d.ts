import { Request, Response, NextFunction } from 'express';
/**
 * Session recovery middleware
 * If the session is not in memory but the storage is available, try to restore the session from storage
 */
export declare function sessionRestoreMiddleware(req: Request, res: Response, next: NextFunction): Promise<void | Response>;
//# sourceMappingURL=session-restore.d.ts.map