import { Request, Response, NextFunction } from 'express';
/**
 * Exclusive mode middleware
 * Checks if server can accept new requests when in exclusive mode
 */
export declare function exclusiveModeMiddleware(req: Request, res: Response, next: NextFunction): void | Response;
//# sourceMappingURL=exclusive-mode.d.ts.map