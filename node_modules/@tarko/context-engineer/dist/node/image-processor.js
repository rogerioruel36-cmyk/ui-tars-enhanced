/**
* Copyright (c) 2025 Bytedance, Inc. and its affiliates.
* SPDX-License-Identifier: Apache-2.0
*/
"use strict";
var __webpack_require__ = {};
(()=>{
    __webpack_require__.d = (exports1, definition)=>{
        for(var key in definition)if (__webpack_require__.o(definition, key) && !__webpack_require__.o(exports1, key)) Object.defineProperty(exports1, key, {
            enumerable: true,
            get: definition[key]
        });
    };
})();
(()=>{
    __webpack_require__.o = (obj, prop)=>Object.prototype.hasOwnProperty.call(obj, prop);
})();
(()=>{
    __webpack_require__.r = (exports1)=>{
        if ('undefined' != typeof Symbol && Symbol.toStringTag) Object.defineProperty(exports1, Symbol.toStringTag, {
            value: 'Module'
        });
        Object.defineProperty(exports1, '__esModule', {
            value: true
        });
    };
})();
var __webpack_exports__ = {};
__webpack_require__.r(__webpack_exports__);
__webpack_require__.d(__webpack_exports__, {
    ImageProcessor: ()=>ImageProcessor
});
const shared_media_utils_namespaceObject = require("@tarko/shared-media-utils");
function _define_property(obj, key, value) {
    if (key in obj) Object.defineProperty(obj, key, {
        value: value,
        enumerable: true,
        configurable: true,
        writable: true
    });
    else obj[key] = value;
    return obj;
}
class ImageProcessor {
    async compressImagesInQuery(query) {
        try {
            if ('string' == typeof query) return query;
            if (Array.isArray(query)) {
                const compressedQuery = await Promise.all(query.map(async (part)=>{
                    var _part_image_url;
                    if ('image_url' === part.type && (null == (_part_image_url = part.image_url) ? void 0 : _part_image_url.url)) return await this.compressImageUrl(part);
                    return part;
                }));
                return compressedQuery;
            }
            return query;
        } catch (error) {
            console.error('Error compressing images in query:', error);
            return query;
        }
    }
    async compressImageUrl(imagePart) {
        try {
            const imageUrl = imagePart.image_url.url;
            if (!imageUrl.startsWith('data:image/')) return imagePart;
            const base64Data = imageUrl.replace(/^data:image\/\w+;base64,/, '');
            const originalBuffer = Buffer.from(base64Data, 'base64');
            const originalSize = originalBuffer.length;
            const compressedBuffer = await this.compressor.compressToBuffer(originalBuffer);
            const compressedSize = compressedBuffer.length;
            const compressedBase64 = `data:image/webp;base64,${compressedBuffer.toString('base64')}`;
            const compressionRatio = originalSize / compressedSize;
            const compressionPercentage = ((1 - compressedSize / originalSize) * 100).toFixed(2);
            console.log('Image compression stats:', {
                original: (0, shared_media_utils_namespaceObject.formatBytes)(originalSize),
                compressed: (0, shared_media_utils_namespaceObject.formatBytes)(compressedSize),
                ratio: `${compressionRatio.toFixed(2)}x (${compressionPercentage}% smaller)`,
                format: 'webp',
                quality: 80
            });
            return {
                ...imagePart,
                image_url: {
                    url: compressedBase64
                }
            };
        } catch (error) {
            console.error('Error compressing individual image:', error);
            return imagePart;
        }
    }
    constructor(options = {}){
        _define_property(this, "compressor", void 0);
        this.compressor = new shared_media_utils_namespaceObject.ImageCompressor({
            quality: options.quality ?? 5,
            format: options.format ?? 'webp'
        });
    }
}
exports.ImageProcessor = __webpack_exports__.ImageProcessor;
for(var __webpack_i__ in __webpack_exports__)if (-1 === [
    "ImageProcessor"
].indexOf(__webpack_i__)) exports[__webpack_i__] = __webpack_exports__[__webpack_i__];
Object.defineProperty(exports, '__esModule', {
    value: true
});

//# sourceMappingURL=image-processor.js.map