/**
 * FIXME: migrate to GUI-Agent package
 *
 * GUI Agent types for Tarko Agent Web UI display.
 * These types are specifically designed for UI rendering and are not part of the internal GUIAgent protocol.
 */
export declare namespace GUIAgent {
    /**
     * Base interface for all GUI Agent action types
     * Defines the fundamental structure that all actions must follow
     */
    interface BaseAction<T extends string = string, I extends Record<string, any> = Record<string, any>> {
        type: T;
        inputs: I;
    }
    /**
     * Click action with coordinates
     */
    type ClickAction = BaseAction<'click', {
        startX: number;
        startY: number;
    }>;
    /**
     * Double click action with coordinates
     */
    type DoubleClickAction = BaseAction<'double_click' | 'left_double', {
        startX: number;
        startY: number;
    }>;
    /**
     * Right click action with coordinates
     */
    type RightClickAction = BaseAction<'right_click' | 'right_single', {
        startX: number;
        startY: number;
    }>;
    /**
     * Drag action with start and end coordinates
     */
    type DragAction = BaseAction<'drag', {
        startX: number;
        startY: number;
        endX: number;
        endY: number;
    }>;
    /**
     * Type action with text content
     */
    type TypeAction = BaseAction<'type', {
        content: string;
    }>;
    /**
     * Hotkey action with key combination
     */
    type HotkeyAction = BaseAction<'hotkey', {
        key: string;
    }>;
    /**
     * Scroll action with coordinates and direction
     */
    type ScrollAction = BaseAction<'scroll', {
        startX: number;
        startY: number;
        direction: 'up' | 'down' | 'left' | 'right';
    }>;
    /**
     * Wait action with no inputs
     */
    type WaitAction = BaseAction<'wait', Record<string, never>>;
    /**
     * Navigate action with URL
     */
    type NavigateAction = BaseAction<'navigate', {
        url: string;
    }>;
    /**
     * Navigate back action
     */
    type NavigateBackAction = BaseAction<'navigate_back', Record<string, never>>;
    /**
     * Union type of all possible GUI actions
     */
    type Action = ClickAction | DoubleClickAction | RightClickAction | DragAction | TypeAction | HotkeyAction | ScrollAction | WaitAction | NavigateAction | NavigateBackAction;
    /**
     * Generic GUI Agent tool response with strict typing
     */
    interface ToolResponse<T extends Action = Action> {
        /**
         * Whether the operation was successful
         */
        success: boolean;
        /**
         * Raw action string as received from the model
         */
        action: string;
        /**
         * Parsed and normalized action with strict typing
         */
        normalizedAction: T;
        /**
         * Optional observation after the action (reserved for future implementation)
         */
        observation?: string;
        /**
         * Error message if the operation failed
         */
        error?: string;
    }
    /**
     * Type-specific response types for better type safety
     */
    type ClickResponse = ToolResponse<ClickAction>;
    type DoubleClickResponse = ToolResponse<DoubleClickAction>;
    type RightClickResponse = ToolResponse<RightClickAction>;
    type DragResponse = ToolResponse<DragAction>;
    type TypeResponse = ToolResponse<TypeAction>;
    type HotkeyResponse = ToolResponse<HotkeyAction>;
    type ScrollResponse = ToolResponse<ScrollAction>;
    type WaitResponse = ToolResponse<WaitAction>;
    type NavigateResponse = ToolResponse<NavigateAction>;
    type NavigateBackResponse = ToolResponse<NavigateBackAction>;
}
/**
 * Legacy action inputs interface for backward compatibility
 * @deprecated Use the new GUIAgent types instead
 */
export interface ActionInputs {
    content?: string;
    start_box?: string;
    end_box?: string;
    key?: string;
    hotkey?: string;
    direction?: string;
    start_coords?: [number, number] | [];
    end_coords?: [number, number] | [];
}
/**
 * Legacy parsed prediction interface for backward compatibility
 * @deprecated Use the new GUIAgent types instead
 */
export interface PredictionParsed {
    /** Action inputs parsed from action_type(action_inputs) */
    action_inputs: ActionInputs;
    /** Action type parsed from action_type(action_inputs) */
    action_type: string;
    /** Thinking content */
    thought?: string;
}
//# sourceMappingURL=gui-agent.d.ts.map